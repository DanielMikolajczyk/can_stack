{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CAN Stack",
            "type": "cppvsdbg", // Use "cppdbg" and "MIMode": "gdb" if on Linux/MinGW
            "request": "launch",
            "program": "${workspaceFolder}/build/output/cantp_node.exe",
            "args": [],
            "stopAtEntry": true,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal"
        }
    ]
}